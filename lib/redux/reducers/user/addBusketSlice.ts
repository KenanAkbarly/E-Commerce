import { createSlice } from "@reduxjs/toolkit";
const initialState = {}

export const  addBusketSlice = createSlice ({
    name:'addSlice',
    initialState,
    reducers:{},
})
export const {} = addBusketSlice.actions;

export default addBusketSlice.reducer;